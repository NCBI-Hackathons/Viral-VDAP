#!/bin/bash

if [ $1 == rmdup ] ; then
    ##remove duplicates
    for i in jsc bc bcbl ;
    do
	(samtools fixmate -r -m -O bam ~/data/align/$i.sorted.bam ~/data/align/$i.rmd.sorted.bam 
	samtools markdup -r -s -S ~/data/align/$i.rmd.sorted.bam ~/data/align/$i.nodup.sorted.bam) &
    done
fi
